# Context — Desktop Pet Application

## Project Background
The project is a Flutter-based Desktop Pet application running on Windows.

## Key Requirements Summary
1. **Custom Pet Avatar (R1)**:
   - Replace WhatsApp Sticker export feature with a "Custom Pet Avatar" setting.
   - User uploads a local PNG/JPG image saved to Storage.
   - Completely replaces default Rive animation (the car).
   - Custom image roams around the screen exactly like the original pet (`pet_window.dart`).
2. **Global Application Review (R2)**:
   - Review entire app codebase.
   - Fix rendering errors (UI overflow warnings).
   - Fix runtime exceptions across any file.
3. **Acceptance Criteria**:
   - App compiles and runs via `flutter run -d windows`.
   - Custom avatar works locally.
   - No unhandled exceptions or overflow warnings in logs.

## Active Stakeholders & Agents
- Parent Agent: 6b39a242-1114-4410-8360-431409c85a9f
- Orchestrator: Desktop Pet Project Orchestrator
